[project]
name = "pyfolio"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["core", "rich>=14.3.3", "typer>=0.27.2"]

[project.scripts]
pyfolio = "pyfolio.main:main"

[build-system]
requires = ["hatchling"]          # Or "flit_core", "setuptools"
build-backend = "hatchling.build"

[tool.uv.sources]
core = { workspace = true }
