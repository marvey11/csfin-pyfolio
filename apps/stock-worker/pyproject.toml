[project]
name = "stock-worker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "config",
    "core",
    "typer>=0.27.1",
]

[project.scripts]
stock-worker = "stock_worker.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
config = { workspace = true }
core = { workspace = true }
